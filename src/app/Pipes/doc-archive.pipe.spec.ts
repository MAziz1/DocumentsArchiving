import { DocArchivePipe } from './doc-archive.pipe';

describe('DocArchivePipe', () => {
  it('create an instance', () => {
    const pipe = new DocArchivePipe();
    expect(pipe).toBeTruthy();
  });
});
